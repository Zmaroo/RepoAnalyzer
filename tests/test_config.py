"""Tests for configuration management."""

import pytest
from config import (
    PostgresConfig,
    Neo4jConfig,
    ParserConfig,
    postgres_config,
    neo4j_config,
    parser_config,
    validate_configs
)
# ... rest of the file remains unchanged 